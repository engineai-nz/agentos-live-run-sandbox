// A deliberately small file for an agent to change.
function greet(name) {
  return "Hello, " + name;
}

function farewell(name) {
  return "Goodbye, " + name;
}

module.exports = { greet, farewell };
