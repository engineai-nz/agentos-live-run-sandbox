# agentos-live-run-sandbox

Throwaway sandbox for AgentOS's first live cloud run. Nothing here is real work.
Safe to delete.

## Usage

```js
const { greet, farewell } = require("./greet");

greet("Ada");    // "Hello, Ada"
farewell("Ada"); // "Goodbye, Ada"
```
